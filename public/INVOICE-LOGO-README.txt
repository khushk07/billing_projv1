Put your shop logo here as:

  store-logo.png

(or change the filename in lib/storeConfig.ts → logoPath)

Tips:
- PNG with transparent background looks best
- About 200×200 pixels is enough
- If you don't want a logo, set logoPath: "" in lib/storeConfig.ts
